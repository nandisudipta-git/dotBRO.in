# Copy to .env and fill in. docker-compose reads this automatically.
# Real secrets never get committed.

# Postgres passwords
AUTH_DB_PASSWORD=bro
REGISTRY_DB_PASSWORD=bro

# Auth JWT signing secret (HS256 for now — swap to asymmetric keys later)
AUTH_JWT_SECRET=dev-secret-change-me