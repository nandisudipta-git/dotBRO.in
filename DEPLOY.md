# Deployment

**Live site:** https://www.dotbro.in
**How it deploys:** GitHub Pages, custom domain via the `CNAME` file (`www.dotbro.in`).
**Repo:** https://github.com/ronnandy56211-byte/dotbro.git (branch `main`)
**Served file:** `index.html` (the card-stack "Human Needs Framework" landing).

Push to `main` → GitHub Pages rebuilds and serves the new `index.html`.

---

## Archived: retired Vercel project (for reference only)

An earlier iteration was linked to Vercel. That deployment path is **no longer used**
(the live site is GitHub Pages + CNAME). IDs kept here in case the Vercel project
needs to be reclaimed or deleted:

- projectName: `dotbro`
- projectId:  `prj_8dckLYRanbSPqBygQYqwZAu5C4xB`
- orgId:      `team_6J97o78JsI4a5AMCneFh3GlM`
